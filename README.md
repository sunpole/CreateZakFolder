# CreateZakFolder

Windows PowerShell utility for creating an order folder structure and transliterating Russian names to Latin characters.

## Run

Open PowerShell in the working directory and run `./CreateZakFolder.ps1`.
